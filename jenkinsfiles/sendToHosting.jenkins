#!groovy
properties([disableConcurrentBuilds()])

pipeline{
    agent{
        label "master"
    }
    options {
        buildDiscarder(logRotator(numToKeepStr: "10", artifactNumToKeepStr: "10"))
        timestamps()
    }  
    stages{
        stage("Auth"){
            steps{
                sh "ssh mdranev@mdranev.beget.tech"
                sh '''
                    hostname
                '''
            }           
        }
    }
}